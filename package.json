{
  "name": "wecode",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "build:web": "npm run build -w web",
    "lint:web": "npm run lint -w web",
    "build:extension": "node extension/build.js --env=dev",
    "build:extension:prod": "node extension/build.js --env=prod"
  }
}
